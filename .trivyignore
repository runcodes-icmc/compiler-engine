# Trivy ignore file — findings listed here are skipped by all scans.
# Lines starting with `#` are comments. Add one finding ID per line, e.g.:
#
#   CVE-2024-12345  # reason for ignoring
#   AVD-DS-0002     # reason for ignoring

# we need to ignore this because the container needs to run as root so it has
# access to the host's docker socket. This is a known security risk,
# but it is necessary for the functionality of the application.
AVD-DS-0002
